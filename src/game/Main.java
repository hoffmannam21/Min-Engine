package game;

import java.util.concurrent.TimeUnit;

public class Main {
	public static void main(String args[]) {
		Env gameEnv = new Env();
		// gameEnv.load(level1);
		//   loads all the characters, scenery into memory
		//   starts their tick cycles
		// gameEnv.addPlayer(mainboy);
		// gameEnv.start();
		//   moves camera to level location, pinned to focused character
		// 
	}
}
